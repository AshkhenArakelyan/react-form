import React from 'react';
import Form from '../../containers/Form/Form';
import './Layout.css'

const Layout = () => {
    return (
        <main className="main">
            <Form />
        </main>
    );
}

export default Layout;